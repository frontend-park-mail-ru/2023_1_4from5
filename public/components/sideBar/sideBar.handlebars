<div id="sideBar">
    <img id="logo" src="../../images/logo.png" alt="logo">
    <div id="sideBar-menu">
<!--        {{#if feed.showDisplay}}-->
<!--            <a id={{feed.id}} href={{feed.href}}> {{feed.name}} </a>-->
<!--        {{/if}}-->
        {{#if findAuth.showDisplay}}
            <a id={{findAuth.id}} href={{findAuth.href}}> {{findAuth.name}} </a>
        {{/if}}
<!--        {{#if subs.showDisplay}}-->
<!--            <a id={{subs.id}} href={{subs.href}}> {{subs.name}} </a>-->
<!--        {{/if}}-->
        {{#if reg.showDisplay}}
            <a id={{reg.id}} href={{reg.href}}> {{reg.name}} </a>
        {{/if}}
        {{#if auth.showDisplay}}
            <a id={{auth.id}} href={{auth.href}}> {{auth.name}} </a>
        {{/if}}
        {{#if beAuthor.showDisplay}}
            <a id={{beAuthor.id}} href={{beAuthor.href}}> {{beAuthor.name}} </a>
        {{/if}}
        {{#if modalWindow.showDisplay}}
            <a id={{modalWindow.id}} href={{modalWindow.href}}> {{modalWindow.name}} </a>
        {{/if}}
    </div>
</div>
